---
const { title, description, technologies } = Astro.props;
---

<div class="p-8">
    <div class="border-blue-secondary border-solid border p-4 rounded-md bg-gradient-to-t to-blue-400">
        <h1 class="text-2xl font-bold">{title}</h1>
        <p class="text-sm">{description}</p>
        <p class="text-sm">{technologies}</p>
    </div>
</div>
