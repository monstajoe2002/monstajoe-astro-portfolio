---
const { title, description, tech, url } = Astro.props;
---

<a target="_blank" href={url}>
  <div
    class=" h-72 shadow-lg dark:from-blue-primary dark:to-blue-500 from-blue-400 to-blue-300 p-6 rounded-md bg-gradient-to-r hover:shadow-xl hover:scale-105 transition duration-500 ease-in-out sm:max-w-2xl"
  >
    <h1 class="text-2xl font-bold text-blue-100 mb-4">{title}</h1>
    <p class="text-md text-light-blue pb-4">{description}</p>

    <div class="mb-6 text-light-blue">
      Built using:

      <p class="text-blue-100 text-md font-semibold">{tech.join(", ")}</p>
    </div>
  </div>
</a>
